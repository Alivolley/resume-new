import { css } from "styled-components";
import vazir from "./../assets/Fonts/Vazir-Regular.ttf";

const Fonts = css`
   @font-face {
      font-family: "vazir";
      src: url(${vazir});
   }
`;

export default Fonts;
